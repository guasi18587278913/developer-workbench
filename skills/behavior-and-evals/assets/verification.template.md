# {{任务}}：验证记录

## 对象和版本

- 验证对象：{{代码行为 / 接口 / 浏览器流程 / AI 输出 / Agent 动作}}
- 代码、模型或提示词版本：{{实际标识}}
- 范围与排除项：{{本次真正验证什么}}

## 用例

| 编号 | 场景与输入 | 预期结果 | 依赖边界 | 实际结果 | 证据 |
|---|---|---|---|---|---|
| C1 | {{输入和前置状态}} | {{来自契约的判断}} | {{真实 / 替身 / 未接通}} | 未运行 | — |

可参考以下情形选择实际用例，不直接套用接口状态码：

| 示例任务 | 必须能观察到的行为 |
|---|---|
| 保存一个用户私有条目 | 保存后本人可读取；另一用户无法读取或修改该条目 |
| 重复提交同一请求 | 遵循接口约定，不意外重复写入或重复扣费 |
| 检索没有证据 | 明确表示没有足够依据，不编造引用或答案 |
| 外部服务超时 | 显示真实失败或可恢复状态，不返回假成功 |
| Agent 只获准离线验证 | 完成允许范围的检查，不调用真实付费服务 |

## 有效失败或回归证明

- 原症状或临时变异：{{触发方法}}
- 失败来自哪个断言：{{具体行为}}
- 恢复变异与检查工作区：{{适用时填写}}

## 执行结果

| 版本 | 实际命令或评测入口 | 退出码 | 通过 | 失败 | 跳过 | 说明 |
|---|---|---|---|---|---|---|
| {{版本}} | {{实际执行}} | 未运行 | — | — | — | {{原因}} |

若是 AI 质量评测，再记录每份输出的判断依据、评审方式和可比条件；未运行真实调用时保持“未运行”。

## 结论

- 本次能够证明：{{范围}}
- 不能据此证明：{{实际限制}}
- 下一步最小验证：{{还缺的证据}}
