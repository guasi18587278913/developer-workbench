# {{能力名称}}：接入契约

## 用户任务与现有入口

- 用户期望的结果：{{具体任务}}
- 复用入口及版本：{{接口、模块或工具位置}}
- 消费方：{{页面、Agent 或业务模块}}
- 已验证状态：{{文档存在 / 本地可用 / 真实服务已验证}}

## 数据与执行边界

| 项目 | 约定 |
|---|---|
| 调用身份与可见范围 | {{实际主体与数据边界}} |
| 输入 | {{字段、类型、必填项及约束}} |
| 输出 | {{成功结果的结构}} |
| 来源证据 | {{来源位置、标识、版本或时间、相关片段}} |
| 可能的副作用 | {{读写、外部调用、费用；没有则明确无}} |
| 重复请求语义 | {{是否可安全重试，依据是什么}} |

## 状态与消费行为

| 状态 | 上游证据 | 下游如何处理 |
|---|---|---|
| 有足够结果 | {{有效结果}} | {{正常消费}} |
| 无匹配资料 | {{真实空结果}} | {{说明缺口或采用已约定路径}} |
| 无权读取 | {{权限判断}} | {{不泄露内容的反馈}} |
| 依赖失败 | {{错误或超时}} | {{失败提示、恢复入口}} |
| 输出非法 | {{校验失败}} | {{拒绝执行并记录原因}} |

## 最小验证

1. {{一条获准输入经过完整链路}}
2. {{另一归属主体无法读取私有结果}}
3. {{无资料、失败或非法输出的相关场景}}
4. {{有副作用时验证重复请求语义}}

## 结果

- 验证版本与证据：{{实际记录}}
- 仍未验证的范围：{{内容}}
